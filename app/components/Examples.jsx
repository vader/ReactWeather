var React = require('react');

var Examples = (props) => {
  return (
    <div>
     <h4>Examples component</h4>
     <p>
       Welcome to components
     </p>
     </div>
  );
};

module.exports = Examples;
